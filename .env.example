# =====================================================================
#  MNPIEPL Backend — Environment Variables
#  Copy this file to .env and fill in your real values.
#  NEVER commit the real .env file to version control.
# =====================================================================

# ── Firebase (Admin SDK — server-side, KEEP SECRET) ──────────────────
# Path to the service account JSON downloaded from Firebase Console
# Firebase Console → Project Settings → Service Accounts → Generate new private key
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# ── Firebase (Client-side PUBLIC keys) ───────────────────────────────
# These are safe to expose to browsers via GET /api/firebase-config
# Firebase Console → Project Settings → Your apps → Web app → SDK setup
FIREBASE_API_KEY=AIzaSyDGhhiKMdZxBXUFNGIEoQQ1J23jyaBWcTU
FIREBASE_AUTH_DOMAIN=hanvi-4c419.firebaseapp.com
FIREBASE_PROJECT_ID=hanvi-4c419
FIREBASE_STORAGE_BUCKET=hanvi-4c419.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=656130761340
FIREBASE_APP_ID=1:656130761340:web:866d374e11a70a60da1ff9
FIREBASE_DATABASE_URL=https://hanvi-4c419-default-rtdb.firebaseio.com/

# ── SMTP Email (server-side, KEEP SECRET) ────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_USE_TLS=True

# ── Client / Business Email ───────────────────────────────────────────
CLIENT_EMAIL=info@mnpiepl.com
CLIENT_NAME=MNPIEPL

# ── Flask Settings ────────────────────────────────────────────────────
FLASK_DEBUG=False
# Comma-separated list of allowed frontend origins (use * for development)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
